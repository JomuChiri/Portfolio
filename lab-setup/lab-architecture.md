📝 Lab Architecture Case Study

1. Title: **SOC Lab Architecture **

2. Objective
• To build a modular SOC environment for practicing detection engineering and incident response.”)

3. Environment Overview
• 	Tools/Platforms: VirtualBox, pfSense, Wazuh Manager, Windows 11 endpoint, Kali Linux attacker- 
- Topology Diagram: Insert or sketch your network layout (firewall, manager, endpoints, attacker VM).
- Network Segmentation: Internal vs external networks, DNS/firewall rules.

4. Setup Steps
- VM creation (OS, resources, adapters).
- pfSense installation and configuration.
- Wazuh manager deployment.
- Endpoint setup (Windows, Sysmon installation planned).
- Kali attacker VM setup.

5. Challenges & Fixes
- Example: “pfSense package installation failed due to cache errors → fixed by clearing cache and re‑adding repo.”
- Example: “VirtualBox adapter misconfigured → corrected by assigning internal network and static IPs.”

6. Outcome
- Functional SOC lab with firewall, SIEM, and endpoints ready for telemetry integration.
- Diagram + screenshots showing connectivity and baseline logs.


